export enum EAddressType {
  SHIPPING = 'shipping',
  BILLING = 'billing',
}

export enum ERole {
  USER = 'user',
  ADMIN = 'admin',
}

export enum EProductVariantSize {
  XS = 'XS',
  S = 'S',
  M = 'M',
  L = 'L',
  XL = 'XL',
  XXL = 'XXL',
}
